
# server.port=8181  # properties
server:
  port: 8181

logging:
  level:
    root: info
    com:
      example:
        apibasic:
          req: trace

springdoc:
  swagger-ui:
    path: /myapi.html

#database connection
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/demodb
    username: root
    password: mariadb
